#include <iostream>
#include <string>
#include <vector>
#include <cstdio>
#include <cstdlib>
#include <ctime>
#include <random>
#include <cmath>
#include "src/RSA.h"

int main(void){
    // RSA rsa(3072);
    RSA rsa(128);

    // rsa.testPrimeDetection(BigInt(32063));
    // rsa.testPrimeDetection(BigInt(502507));

    // for(int i = 0; i < 2; i++){
        // rsa.testPrimeGeneration(1024);
    // }
}